Conteúdo do diretório /etc/puppet do puppetmaster do curso Puppet Fundamentals for System Administrators

Nas máquinas de nó, adicionar a seguinte linha na seção [agent] do /etc/puppet/puppet.conf:
server = puppetmaster
